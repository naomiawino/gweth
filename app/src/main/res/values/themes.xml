<resources>

    <!-- Main Theme -->
    <style name="Theme.HospitalManagementSystem" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash Theme -->
    <style name="Theme.HospitalManagementSystem.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="postSplashScreenTheme">@style/Theme.HospitalManagementSystem</item>
    </style>

</resources>